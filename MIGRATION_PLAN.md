# План миграции бэкенда с C# на Node.js

## ✅ Выполнено (Этап 1)

### 1. Структура проекта
- ✅ Создана структура папок Node.js проекта
- ✅ Настроен `package.json` с зависимостями
- ✅ Настроен TypeScript (`tsconfig.json`)
- ✅ Создан `.gitignore` и `.env.example`

### 2. База данных
- ✅ Настроена Prisma схема (`schema.prisma`)
- ✅ Созданы модели: User, Role, Book, Cart, CartItem, Order, OrderItem
- ✅ Настроено подключение к PostgreSQL
- ✅ Создан seed файл для начальных данных

### 3. Конфигурация
- ✅ Настроены переменные окружения
- ✅ Конфигурация приложения (`src/config/app.ts`)
- ✅ Подключение к БД (`src/config/database.ts`)

### 4. Утилиты
- ✅ JWT утилиты (генерация, верификация, обновление токенов)
- ✅ Хеширование паролей (bcryptjs)
- ✅ Кастомные классы ошибок

### 5. Middleware
- ✅ Обработчик ошибок
- ✅ Аутентификация (JWT middleware)
- ✅ Проверка ролей (Admin, User)
- ✅ CORS настройки

### 6. Аутентификация
- ✅ Регистрация пользователей (`POST /api/account/register`)
- ✅ Авторизация (`POST /api/account/login`)
- ✅ Получение профиля (`GET /api/account/profile`)
- ✅ Обновление токена (`POST /api/account/refresh`)
- ✅ Сервис аутентификации (`auth.service.ts`)
- ✅ Контроллер аутентификации (`account.controller.ts`)
- ✅ Роуты аутентификации (`account.routes.ts`)

### 7. Основное приложение
- ✅ Express сервер (`src/index.ts`)
- ✅ Health check endpoint
- ✅ Обработка ошибок

---

## 📋 Осталось выполнить

### Этап 2: API для книг (Books API)
- [ ] Сервис книг (`books.service.ts`)
- [ ] Контроллер книг (`books.controller.ts`)
- [ ] Роуты книг (`books.routes.ts`)
- [ ] Endpoints:
  - `GET /api/books` - получить все книги
  - `GET /api/books/:id` - получить книгу по ID
  - `POST /api/books` - создать книгу (Admin)
  - `PUT /api/books/:id` - обновить книгу (Admin)
  - `DELETE /api/books/:id` - удалить книгу (Admin)

### Этап 3: API корзины (Cart API)
- [ ] Сервис корзины (`cart.service.ts`)
- [ ] Контроллер корзины (`cart.controller.ts`)
- [ ] Роуты корзины (`cart.routes.ts`)
- [ ] Endpoints:
  - `GET /api/cart` - получить корзину пользователя
  - `POST /api/cart/items` - добавить товар в корзину
  - `PUT /api/cart/items/:bookId` - обновить количество
  - `DELETE /api/cart/items/:bookId` - удалить товар из корзины
  - `DELETE /api/cart` - очистить корзину

### Этап 4: API заказов (Orders API)
- [ ] Сервис заказов (`orders.service.ts`)
- [ ] Контроллер заказов (`orders.controller.ts`)
- [ ] Роуты заказов (`orders.routes.ts`)
- [ ] Endpoints:
  - `GET /api/orders` - получить заказы пользователя
  - `POST /api/orders` - создать заказ из корзины
  - `GET /api/orders/:id` - получить заказ по ID
  - `POST /api/orders/:id/confirm-delivery` - подтвердить доставку

### Этап 5: Профиль пользователя (User Profile API)
- [ ] Сервис профиля (`profile.service.ts`)
- [ ] Контроллер профиля (`profile.controller.ts`)
- [ ] Роуты профиля (`profile.routes.ts`)
- [ ] Endpoints:
  - `GET /api/user/profile` - получить профиль
  - `PUT /api/user/profile` - обновить профиль
  - `GET /api/user/stats` - получить статистику пользователя

### Этап 6: Админ панель (Admin API)
- [ ] Сервис администратора (`admin.service.ts`)
- [ ] Контроллеры админа:
  - `admin.books.controller.ts` - управление книгами
  - `admin.users.controller.ts` - управление пользователями
  - `admin.orders.controller.ts` - управление заказами
  - `admin.carts.controller.ts` - просмотр корзин
  - `admin.stats.controller.ts` - статистика
- [ ] Роуты админа (`admin.routes.ts`)
- [ ] Endpoints:
  - Управление пользователями: CRUD операции
  - Управление книгами: CRUD операции
  - Управление заказами: изменение статусов
  - Статистика: общая статистика системы

### Этап 7: Дополнительно
- [ ] Swagger документация
- [ ] Валидация запросов (Zod schemas)
- [ ] Тесты (опционально)
- [ ] Логирование (Winston/Pino)
- [ ] Rate limiting

---

## 🚀 Как начать работу

### 1. Установка зависимостей
```bash
cd leafside-backend-node
npm install
```

### 2. Настройка базы данных
```bash
# Убедитесь, что PostgreSQL запущен (из LeafSide-backend)
cd ../LeafSide-backend
docker-compose up -d

# Вернитесь в Node.js проект
cd ../leafside-backend-node

# Создайте .env файл
cp .env.example .env

# Генерация Prisma Client
npm run prisma:generate

# Применение миграций
npm run prisma:migrate

# Заполнение начальными данными
npm run prisma:seed
```

### 3. Запуск приложения
```bash
# Режим разработки
npm run dev

# Production
npm run build
npm start
```

---

## 📝 Примечания

- Все эндпоинты должны соответствовать C# версии API
- Используется та же база данных PostgreSQL
- JWT токены имеют тот же формат для совместимости с фронтендом
- Структура ответов API идентична C# версии

---

## 🔄 Следующие шаги

Рекомендую продолжить с **Этапа 2 (Books API)**, так как это базовый функционал, необходимый для работы приложения.

