# LeafSide Backend API (Node.js)

REST API для интернет-магазина книг LeafSide, построенный на Node.js с использованием Express.js и TypeScript.

## 🏗 Архитектура

Проект следует принципам Clean Architecture и разделен на следующие слои:

- **src/controllers** - API контроллеры
- **src/services** - Бизнес-логика и сервисы
- **src/models** - Модели данных (Prisma)
- **src/middleware** - Middleware (аутентификация, валидация, обработка ошибок)
- **src/utils** - Утилиты и хелперы
- **src/config** - Конфигурация приложения

## 🚀 Технологический стек

- **Node.js** - runtime environment
- **Express.js** - веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **Prisma** - ORM для работы с PostgreSQL
- **PostgreSQL** - база данных
- **JWT** - аутентификация и авторизация
- **Swagger** - документация API

## 📁 Структура проекта

```
leafside-backend-node/
├── src/
│   ├── controllers/          # API контроллеры
│   ├── services/              # Бизнес-логика
│   ├── middleware/            # Middleware
│   ├── models/               # Prisma модели
│   ├── utils/                # Утилиты
│   ├── config/               # Конфигурация
│   └── index.ts              # Точка входа
├── prisma/
│   ├── schema.prisma         # Схема базы данных
│   └── seed.ts              # Сидер для начальных данных
├── package.json
├── tsconfig.json
└── .env
```

## 🔧 Настройка и запуск

### Предварительные требования
- Node.js 18+
- PostgreSQL (можно использовать Docker)
- npm или yarn

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка базы данных
```bash
# Запуск PostgreSQL в Docker (из родительской папки)
cd ../LeafSide-backend
docker-compose up -d

# Или используйте существующую базу данных
```

### 3. Настройка переменных окружения
```bash
cp .env.example .env
# Отредактируйте .env файл при необходимости
```

### 4. Настройка Prisma
```bash
# Генерация Prisma Client
npm run prisma:generate

# Применение миграций
npm run prisma:migrate

# Заполнение начальными данными (опционально)
npm run prisma:seed
```

### 5. Запуск приложения
```bash
# Режим разработки
npm run dev

# Production режим
npm run build
npm start
```

API будет доступен по адресу: `http://localhost:5233`

## 📚 API Документация

После запуска приложения документация доступна по адресу:
- Swagger UI: `http://localhost:5233/api-docs`

## 🗄 Модель данных

Структура базы данных идентична C# версии проекта:
- **User** - пользователи системы
- **Book** - книги
- **Cart** - корзины пользователей
- **CartItem** - элементы корзины
- **Order** - заказы
- **OrderItem** - элементы заказов

## 🔒 Безопасность

- JWT токены для аутентификации
- Роли пользователей (User, Admin)
- Валидация входных данных через Zod
- CORS настройки
- Хеширование паролей через bcryptjs

